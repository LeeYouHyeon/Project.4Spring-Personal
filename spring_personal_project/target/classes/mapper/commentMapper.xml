<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koreait.www.repository.CommentDAO">
	<select id="getList" resultType="com.koreait.www.domain.CommentVO">
		select * from comment where bno=#{bno} order by cno
	</select>
	<insert id="register">
		<if test="pwd != null">
			insert into comment(bno, writer, pwd, content)
			values(#{bno}, #{writer}, #{pwd}, #{content})
		</if>
		<if test="pwd == null">
			insert into comment(bno, writer, name, content)
			values(#{bno}, #{writer}, #{name}, #{content})
		</if>
	</insert>
</mapper>